---
description: Dotfiles repo conventions for stow and AI config placement
alwaysApply: true
---

This is a macOS dotfiles repo managed with GNU Stow. The canonical description of
the repo layout — stow-package structure, where new files go (stow package vs
`.cursor/rules/` vs gitignored output), and the AI config packages — lives in
@CLAUDE.md under "Architecture ▸ Stow Package Structure". Read that before
creating files; do not restate it here.
